---
'@cogenta/core': minor
---

Add one error code for L4's memory layer:
`AGENT_APPROVAL_NOT_DECIDED` (converting a still-pending approval request
into a memory record — only a decided one carries a learning signal).
