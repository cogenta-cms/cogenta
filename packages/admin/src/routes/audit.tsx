import type { JSX } from 'react'

/** Placeholder — filtering and pagination over @cogenta/auth's audit log land in task 14. */
export function AuditRoute(): JSX.Element {
  return (
    <section aria-labelledby="audit-heading">
      <h1 id="audit-heading">Journal d'audit</h1>
      <p>À venir : liste consultable et filtrable des actions.</p>
    </section>
  )
}
