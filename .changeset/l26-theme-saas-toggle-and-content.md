---
'@cogenta/theme-saas': minor
---

Wire the new `renderThemeToggle` into the header, styled as `.cg-theme-toggle` in this theme's own pill/border register. Every page now offers a manual light/dark/system control; the CSS to support it already existed.
