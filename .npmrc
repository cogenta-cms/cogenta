engine-strict=true
provenance=true

# Third-party install scripts do not run by default: pnpm only builds the packages
# listed in `onlyBuiltDependencies` (pnpm-workspace.yaml). Enabling one is an explicit,
# reviewed decision (rules R9 and R10).
