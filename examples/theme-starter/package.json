{
  "name": "@cogenta/example-theme-starter",
  "version": "0.0.0",
  "private": true,
  "description": "A real, minimal Cogenta theme — the downloadable starting point docs-site's \"Créer un thème\" guide links to. Checked against contract D by its own test, so it cannot silently rot.",
  "license": "MPL-2.0",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cogenta/blocks": "workspace:*"
  },
  "peerDependencies": {
    "astro": "catalog:"
  },
  "devDependencies": {
    "@cogenta/render": "workspace:*",
    "astro": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
