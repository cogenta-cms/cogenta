You are working in the Cogenta monorepo (pnpm workspaces, TypeScript strict, ESM only, Vitest, Biome), in YOUR OWN git worktree on your own branch (from origin/main). Read `CLAUDE.md` and `AGENTS.md` first (R1–R10 are non-negotiable), then `docs/lots/L25-templates-pro.md` in full — especially "Ce que tout template partage", "Pièges connus", and the Annexe brief for YOUR theme (quoted below). You are one of five parallel agents each building one site-type template; four other agents are doing the same for other types, so stay inside your file scope.

## What already exists (Phase 0, merged into origin/main — use it, do not rebuild it)
- `@cogenta/theme-kit` (`packages/theme-kit/src`): contract D `theme@1.4` — `ChromeInput.tagline/social/footerNote/headerAction` + `renderSocialLinks()`, `renderBrandMark()`; `PageContent.entry` (`PageEntryMeta`: publishedAt, updatedAt, image, excerpt, author, terms, readingMinutes) + `renderEntryHeader(page, ctx)`; `entryImage(entry, ctx)`; `renderIcon(name)` (~50 outline icons, see `icons.ts` for names); `createThemeTranslator`/`THEME_STRINGS` (the strings `ctx.t` resolves — add a key there only if you truly need a new one, in both `en` and `fr`); `image()`, `h()`, `heading()`, `escape*`, `pageHasOwnHeading`, `withBlockKey`, `buildCollectionListQuery`, `renderCommentsSection`, term-archive types.
- A reference implementation of ALL of the above: `packages/theme-canonical` (read its `src/render/chrome.ts`, `render-block.ts`, `blocks/feature-grid.ts`, `blocks/collection-list.ts`, `styles/*.css`, and its tests). The four L23 themes (`theme-portfolio`, `theme-magazine`, `theme-ecommerce`, `theme-entreprise`) show the package skeleton to copy exactly: `package.json` (exports `.`, `./theme.config`, `./tokens.json`, `./styles/*.css`), `theme.config.ts` (seventeen blocks in contract order), `tokens.json`, `src/styles/{tokens,base,blocks,archive,theme}.css`, `src/render/{chrome,render-block,term-archive}.ts`, `src/render/blocks/*.ts` (17 files), `test/` (`css-color.ts` evaluator, `design-system.test.ts` contrast in light AND dark computed from the real stylesheet, `font-display.test.ts`, `isolation.test.ts` (no `<script>`, no `on*`), `page.test.ts`, `tokens.test.ts`, `chrome.test.ts`, `chrome-brand.test.ts`, one test per block).
- `create-cogenta` (`packages/create-cogenta/src`): `blueprints/<type>.ts` content packs; `BlueprintContentPack` now has `defaultTheme`, `menus`, `siteSettings`, `mediaSpecs` and `seedDemoContent(ctx: SeedContext)` where `ctx.media` maps media spec names → media ids; `blueprints/store.ts` is the finished example to copy (default theme, hero with `media`, entries with an image field, header/footer/`header-action` menus, `general.tagline`/`general.socialLinks` (`{label, url}`)/`general.footerNote`). Demo visuals: `src/demo-art/` — `heroArt(palette, variant, seed)`, `coverArt(palette, seed)`, `avatarArt(palette, seed)`, `logoArt(seed)`, `productArt(palette, seed)`; `palette` is a `SkinTokens['color']` — add your blueprint's palette to `blueprints/starting-skins.ts` if it has none (it must pass `validateSkin`, there is a test). Another agent is improving demo-art's internals right now; the signatures above are stable, do not edit `demo-art/`.
- `packages/cli/src/commands/theme-registry.ts` lists themes; `packages/cli/package.json` depends on each theme (`workspace:*`). Add yours to both (one entry each — keep the change minimal, I will resolve merge conflicts).

## Deliverables
1. **The theme package** `packages/<theme-package>` (name below), a genuinely distinct design — never a recolour of canonical — with: all 17 blocks; `renderChrome` using ALL of theme@1.4 (sticky header with brand mark/name, nav, `headerAction` as a button, a CSS-only mobile menu via `<details>` or a checkbox — no JS; footer in columns: brand + tagline, footer nav, social links with icons, footer note, `brandingHtml` placed unaltered); `renderPage` that uses `renderEntryHeader` (or draws its own header from `page.entry`, if the brief says so) and never emits a duplicate `<h1>`; `renderTermArchive`; cards in `collectionList` that show `entryImage` when present (aspect-ratio box, `object-fit: cover`, `loading="lazy"`), excerpt and date; feature icons via `renderIcon`; tasteful `:hover`/`:focus-visible`; `prefers-reduced-motion`; a designed dark mode with `light-dark()`/`oklch(from …)` (copy the technique from `theme-canonical`'s `tokens.css`); mobile-first CSS verified at 360/768/1280; Google Fonts via `@import` only (with real fallback stacks); zero literal colours in CSS (test); WCAG AA computed by test in both schemes; ≥150 tests.
2. **The blueprint** `packages/create-cogenta/src/blueprints/<type>.ts` (+ its test file): `defaultTheme`, the home page composition from the brief (8–12 blocks with real, specific, English demo copy — no lorem ipsum), demo entries PUBLISHED with cover images from `coverArt`/`productArt`/`avatarArt`/`logoArt` via `mediaSpecs`, the `coverImage`/`photo`/`icon` fields the brief names, menus (`header`, `footer`, `header-action`), `siteSettings` (tagline, 3 social links, footer note), and a starting skin whose palette matches the theme's identity.
3. Registry + cli dependency entries; a changeset for the new theme package (`minor`), for `@cogenta/cli` (`patch`, "registers …"), and for `create-cogenta` (`minor`).

## Verification you MUST run and report with real output (no exceptions)
1. `pnpm install --offline` (link the new package), then `pnpm -F <theme-package> typecheck && pnpm -F <theme-package> test && pnpm -F <theme-package> build`; `pnpm -F create-cogenta typecheck && pnpm -F create-cogenta test -- <type>`; `pnpm -F @cogenta/cli typecheck`; `pnpm exec biome check` on all touched files; `pnpm turbo run typecheck --force --filter=@cogenta/cli...`.
2. **A real site, looked at**: build (`pnpm turbo run build --filter=create-cogenta... --filter=@cogenta/cli...`), scaffold into a scratch directory OUTSIDE the repo with `node packages/create-cogenta/dist/bin.js <dir> --config <json>` (read `src/config-file.ts` for the JSON keys: `blueprint`, `site.name`, `database.driver: "sqlite"`, `adminEmail`/password if required) — do NOT run npm install in it; then serve it with the workspace's own cli, which already has every theme linked: from the scratch dir, `node <repo>/packages/cli/dist/bin.js serve` on a free port (read `packages/cli/src/commands/serve.ts` for the port option/env). Then use the Chrome browser tools (`mcp__claude-in-chrome__*`: `tabs_context_mcp` → `tabs_create_mcp` → `navigate` → `resize_window` → `computer` screenshot) to screenshot the home page, one entry page, and one term archive at widths 360, 768 and 1280, in light mode — and LOOK at every screenshot: overlapping text, a hero without its image, an empty-looking section, a mobile menu that does not open, a footer without columns, are all failures to fix before reporting. Also `curl` the home page and check zero `<script` tags. Stop the server; delete the scratch site.
3. The other four L23 themes and canonical must still pass `typecheck`/`test` (you only added registry entries; prove nothing else moved).

## Constraints
- File scope: `packages/<theme-package>/**` (new), `packages/create-cogenta/src/blueprints/<type>.ts`, `packages/create-cogenta/src/blueprints/starting-skins.ts` (add one key), `packages/create-cogenta/test/<type>-blueprint.test.ts` (+ `starting-skins.test.ts` if needed), `packages/cli/src/commands/theme-registry.ts` (one entry), `packages/cli/package.json` (one dependency line), `.changeset/*` (new files only), `pnpm-lock.yaml` (from `pnpm install`). Do NOT edit `packages/theme-kit`, `theme-canonical`, other themes, `demo-art`, `scaffold.ts`, `content-pack.ts`, `docs/**`.
- No `any`, no `@ts-ignore`, no `console.log`, `CogentaError` for library errors, no new npm dependency (R9), no client JavaScript, no literal colour in CSS, no SVG in the media pipeline (ADR-0017 — inline SVG icons in HTML are fine). Contract B is frozen: no new block types; `collectionList.sort.field` is only `id`/`createdAt`/`updatedAt`.
- Never `git stash`. Never touch `main`. Never push. Commit on your branch with `git commit -s`, Conventional Commits, scope from `commitlint.config.js` (`<theme-scope>`, `create`, `cli`).

## Report back (all I will see)
What you built (design decisions, fonts, palette, layout signature, per-block highlights), every decision the brief left open, exact commands + real results (test counts), what the screenshots showed and what you fixed after looking, anything not done or not verified (honestly), branch name, `git log --oneline origin/main..HEAD`, and the scratch path of your screenshots.
