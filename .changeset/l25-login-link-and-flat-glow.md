---
'@cogenta/admin': patch
---

The admin's own "Cogenta" credit — on the login rail and the shell footer — now links to
the project (`github.com/cogenta-cms/cogenta`), the same way the public site's own footer
credit already does; it previously named Cogenta without linking anywhere. The login
rail's three blurred gradient glows are replaced with flat geometric accents (a solid
ring, a solid disc, a hairline ring — zero gradient, zero decorative blur), matching the
D5 rule already applied to the ten site themes.
