---
'@cogenta/core': minor
---

Add two error codes for L4's autonomy and approval layer:
`TOOL_CALL_REJECTED` (a human rejected a tool call awaiting approval) and
`APPROVAL_REQUEST_UNKNOWN` (deciding an approval request id that does not
exist).
