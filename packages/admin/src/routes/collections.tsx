import type { JSX } from 'react'

/** Placeholder — the schema-driven list and edit views land in tasks 6-9. */
export function CollectionsRoute(): JSX.Element {
  return (
    <section aria-labelledby="collections-heading">
      <h1 id="collections-heading">Contenus</h1>
      <p>À venir : liste générée depuis schema.json, édition, versions.</p>
    </section>
  )
}
