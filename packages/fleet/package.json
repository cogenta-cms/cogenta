{
  "name": "@cogenta/fleet",
  "version": "0.1.3",
  "description": "Multi-site fleet control plane — pairing, telemetry, inventory, rollouts, reporting.",
  "license": "MPL-2.0",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.11.0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cogenta-cms/cogenta.git",
    "directory": "packages/fleet"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cogenta/agents-builtin": "workspace:*",
    "@cogenta/auth": "workspace:*",
    "@cogenta/channels": "workspace:*",
    "@cogenta/core": "workspace:*",
    "@cogenta/plugins": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
