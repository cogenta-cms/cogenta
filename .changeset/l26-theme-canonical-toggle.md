---
'@cogenta/theme-canonical': minor
---

Wire the new `renderThemeToggle` into the header, styled as `.cg-theme-toggle`. Every page now offers a manual light/dark/system control; the CSS to support it already existed.
